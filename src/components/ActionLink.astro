---
interface Props {
  href: string;
}

const { href } = Astro.props;
const isExternal = href.startsWith('http');
---

<a
  href={href}
  class="inline-flex items-center gap-1 text-[#1B63A6]"
  {...(isExternal ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
>
  <slot />
</a>
